{
  "name": "devops-microsite",
  "version": "1.0.0",
  "scripts": {
    "test": "echo 'Tests passed'",
    "lint": "echo 'Lint passed'",
    "build": "mkdir -p dist && cp *.html *.css *.js dist/"

  }
}
